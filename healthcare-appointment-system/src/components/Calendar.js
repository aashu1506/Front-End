import React from 'react';
import { motion } from 'framer-motion';

const Calendar = ({ onDateSelect }) => {
  // Component code here (as provided in the previous message)
};

export default Calendar;
